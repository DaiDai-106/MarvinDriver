# MarvinDriver

基于 DummyServer 中间件的 Marvin 双臂驱动服务，通过 HTTP 接口提供机器人控制功能。

## 项目简介

MarvinDriver 是一个用于控制天机双臂机器人的驱动服务，封装了机器人控制 SDK（Marvin_Robot）和运动学计算模块（Marvin_Kine）

## 功能特性

- 🤖 **双臂机器人控制**：支持左臂（A）和右臂（B）的独立控制
- 🔗 **HTTP 服务接口**：基于 DummyServer 中间件提供 API
- 📊 **状态监控**：实时获取机器人关节位置和笛卡尔坐标
- 🔧 **运动学计算**：集成运动学正逆解算功能
- 📝 **日志记录**：完整的日志系统，便于调试和监控

## 系统要求
- Python >= 3.11
- Linux / Windows / macOS
- 机器人硬件连接
## 安装

### 1. 克隆仓库

```bash
git clone <repository-url>
cd MarvinDriver
```

### 2. 安装依赖
使用 `uv` 包管理器（推荐）：
```bash
uv sync
```
或使用 `pip`：
```bash
pip install -e .
```
### 3. 环境配置
创建 `.env` 文件（如果需要配置环境变量）：
```bash
touch .env
```
## 使用方法
### 启动服务

```bash
python main.py
```

服务将在 `http://0.0.0.0:8010` 启动。

## 项目结构

```
MarvinDriver/
├── main.py                      # 主程序入口
├── pyproject.toml              # 项目配置和依赖
├── README.md                   # 项目文档
├── LICENSE                     # 许可证
└── src/
    └── marvin_driver/
        ├── __init__.py
        ├── marvin_server.py    # 服务主类
        └── marvin/             # 机器人SDK模块
            ├── __init__.py
            ├── robot.py        # 机器人控制类 (Marvin_Robot)
            ├── kinematics.py   # 运动学计算类 (Marvin_Kine)
            ├── gripper.py      # 夹爪控制
            ├── modbus.py       # Modbus通信
            ├── structure.py    # 数据结构定义
            └── lib/            # 动态链接库
                ├── libKine.so
                └── libMarvinSDK.so
        └── config/             # 配置文件
            ├── ccs_m6.MvKDCfg
            └── srs.MvKDCfg
```

## 联系方式

如有问题或建议，请通过 Issue 反馈。
